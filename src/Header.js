import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Nombre de la Aplicación</h1>
      {/* Aquí podrías añadir más elementos, como un logo, botones, etc. */}
    </header>
  );
};

export default Header;